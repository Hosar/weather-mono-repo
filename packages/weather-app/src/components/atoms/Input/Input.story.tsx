import { Input } from "./Input";

const definition = {
  title: "Atoms/Input",
  component: Input,
};

export default definition;


export const InputZipCode = () => {
    return (
        <Input  />
    )
}
